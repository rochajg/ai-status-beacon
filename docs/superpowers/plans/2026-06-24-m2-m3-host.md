# Status Beacon Host — M2 + M3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar a CLI `beacon <state>` com modo `--direct` (M2) e o daemon com Unix socket (M3), de forma que o Claude Code possa disparar estados sem nunca bloquear.

**Architecture:** A CLI tenta primeiro o socket do daemon; se o daemon não estiver rodando e `--direct` for passado, abre a serial diretamente. O daemon mantém a porta serial aberta permanentemente, serializa escritas e reconecta automaticamente se o dispositivo for desplugado.

**Tech Stack:** Python 3.11+, pyserial 3.5+, stdlib (socket, threading, pathlib), pytest 8+

## Global Constraints

- Exit code sempre 0, mesmo em falha — hooks do Claude Code não podem ser bloqueados (RNF5)
- Timeout total de 300ms para qualquer operação de rede/serial (RNF5)
- Porta serial: sempre `cu.usbmodem*` no macOS (nunca `tty.*` — bloqueia aguardando DCD)
- Socket path: `~/.beacon/beacon.sock`
- Baud rate: 115200
- Protocolo: linha de texto terminada em `\n`
- Estados válidos: `thinking`, `waiting`, `done`, `idle`, `error`, `ping`

---

## File Map

```
host/
  beacon/
    __init__.py         (sem mudanças)
    cli.py              (M2: --direct; M3: tenta socket primeiro)
    serialio.py         (M2: send_direct; M3: send_to_socket)
    daemon.py           (M3: socket listener + serial bridge + reconnect)
  tests/
    __init__.py         (criar)
    test_serialio.py    (criar)
    test_cli.py         (criar)
    test_daemon.py      (criar)
  pyproject.toml        (adicionar pytest como dev dependency)
```

---

### Task 1: Setup de testes e pyproject.toml

**Files:**
- Modify: `host/pyproject.toml`
- Create: `host/tests/__init__.py`

- [ ] **Step 1: Adicionar pytest ao pyproject.toml**

Substituir o conteúdo de `host/pyproject.toml`:

```toml
[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "beacon"
version = "0.1.0"
description = "Physical AI status indicator for Claude Code"
requires-python = ">=3.11"
dependencies = [
    "pyserial>=3.5",
]

[project.scripts]
beacon = "beacon.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["beacon*"]

[project.optional-dependencies]
dev = ["pytest>=8"]
```

- [ ] **Step 2: Criar `host/tests/__init__.py`**

Arquivo vazio:
```python
```

- [ ] **Step 3: Instalar o pacote em modo editável com deps de dev**

```bash
cd host && pip install -e ".[dev]"
```

Saída esperada: `Successfully installed beacon-0.1.0`

- [ ] **Step 4: Verificar pytest disponível**

```bash
cd host && python -m pytest --version
```

Saída esperada: `pytest 8.x.x`

- [ ] **Step 5: Commit**

```bash
git add host/pyproject.toml host/tests/__init__.py
git commit -m "chore: add pytest dev dependency and tests package"
```

---

### Task 2: M2 — serialio.send_direct

**Files:**
- Modify: `host/beacon/serialio.py`
- Create: `host/tests/test_serialio.py`

**Produces:**
- `find_port() -> str | None` — já existe
- `send_direct(state: str, port: str, timeout_s: float = 0.3) -> bool` — abre a serial, envia `state\n`, fecha; retorna True se ok

- [ ] **Step 1: Escrever os testes**

Criar `host/tests/test_serialio.py`:

```python
from unittest.mock import patch, MagicMock
import beacon.serialio as sio


def test_find_port_env_override(monkeypatch):
    monkeypatch.setenv("BEACON_SERIAL_PORT", "/dev/fake")
    assert sio.find_port() == "/dev/fake"


def test_find_port_glob(monkeypatch):
    monkeypatch.delenv("BEACON_SERIAL_PORT", raising=False)
    with patch("glob.glob", return_value=["/dev/cu.usbmodem42"]):
        assert sio.find_port() == "/dev/cu.usbmodem42"


def test_find_port_none_when_no_device(monkeypatch):
    monkeypatch.delenv("BEACON_SERIAL_PORT", raising=False)
    with patch("glob.glob", return_value=[]):
        assert sio.find_port() is None


def test_send_direct_writes_state():
    mock_serial = MagicMock()
    with patch("serial.Serial", return_value=mock_serial):
        result = sio.send_direct("thinking", "/dev/cu.fake")
    mock_serial.write.assert_called_once_with(b"thinking\n")
    mock_serial.close.assert_called_once()
    assert result is True


def test_send_direct_returns_false_on_error():
    with patch("serial.Serial", side_effect=OSError("no device")):
        result = sio.send_direct("thinking", "/dev/cu.fake")
    assert result is False


def test_send_direct_closes_port_on_exception():
    mock_serial = MagicMock()
    mock_serial.write.side_effect = OSError("write failed")
    with patch("serial.Serial", return_value=mock_serial):
        result = sio.send_direct("thinking", "/dev/cu.fake")
    mock_serial.close.assert_called_once()
    assert result is False
```

- [ ] **Step 2: Rodar para confirmar que falham**

```bash
cd host && python -m pytest tests/test_serialio.py -v
```

Saída esperada: `FAILED` em `test_send_direct_*` (função não existe ainda)

- [ ] **Step 3: Implementar `send_direct` em `host/beacon/serialio.py`**

Substituir o conteúdo completo:

```python
"""Serial port discovery and direct I/O."""

import glob
import os

import serial


def find_port() -> str | None:
    override = os.environ.get("BEACON_SERIAL_PORT")
    if override:
        return override
    # Use cu.* not tty.* — on macOS, tty.* blocks waiting for DCD signal
    # that MicroPython's USB CDC never asserts.
    candidates = glob.glob("/dev/cu.usbmodem*")
    return candidates[0] if candidates else None


def send_direct(state: str, port: str, timeout_s: float = 0.3) -> bool:
    """Open port, send state\n, close. Returns True on success."""
    s = None
    try:
        s = serial.Serial(port, 115200, timeout=timeout_s, write_timeout=timeout_s)
        s.write(f"{state}\n".encode())
        return True
    except Exception:
        return False
    finally:
        if s is not None:
            try:
                s.close()
            except Exception:
                pass
```

- [ ] **Step 4: Rodar testes — devem passar**

```bash
cd host && python -m pytest tests/test_serialio.py -v
```

Saída esperada: todos `PASSED`

- [ ] **Step 5: Commit**

```bash
git add host/beacon/serialio.py host/tests/test_serialio.py
git commit -m "feat(M2): implement serialio.send_direct with tests"
```

---

### Task 3: M2 — CLI --direct

**Files:**
- Modify: `host/beacon/cli.py`
- Create: `host/tests/test_cli.py`

**Consumes:**
- `serialio.find_port() -> str | None`
- `serialio.send_direct(state: str, port: str, timeout_s: float) -> bool`

**Produces:**
- `main()` — entry point; quando `--direct` é passado, chama `send_direct` e sai 0

- [ ] **Step 1: Escrever os testes**

Criar `host/tests/test_cli.py`:

```python
import sys
import pytest
from unittest.mock import patch
from beacon.cli import main


def run_cli(*args):
    with patch("sys.argv", ["beacon", *args]):
        with pytest.raises(SystemExit) as exc:
            main()
    return exc.value.code


def test_no_args_exits_0():
    assert run_cli() == 0


def test_help_exits_0():
    assert run_cli("--help") == 0


def test_invalid_state_exits_0():
    assert run_cli("flying") == 0


def test_direct_with_device_sends_state():
    with patch("beacon.serialio.find_port", return_value="/dev/cu.fake"), \
         patch("beacon.serialio.send_direct", return_value=True) as mock_send:
        code = run_cli("thinking", "--direct")
    mock_send.assert_called_once_with("thinking", "/dev/cu.fake", timeout_s=0.3)
    assert code == 0


def test_direct_exits_0_when_no_device():
    with patch("beacon.serialio.find_port", return_value=None):
        code = run_cli("done", "--direct")
    assert code == 0


def test_direct_exits_0_when_send_fails():
    with patch("beacon.serialio.find_port", return_value="/dev/cu.fake"), \
         patch("beacon.serialio.send_direct", return_value=False):
        code = run_cli("idle", "--direct")
    assert code == 0
```

- [ ] **Step 2: Rodar para confirmar que falham**

```bash
cd host && python -m pytest tests/test_cli.py -v
```

Saída esperada: `FAILED` em `test_direct_*`

- [ ] **Step 3: Implementar CLI com --direct em `host/beacon/cli.py`**

```python
"""CLI entry point for the Status Beacon."""

import sys

from beacon import serialio

VALID_STATES = {"thinking", "waiting", "done", "idle", "error", "ping"}
SOCKET_TIMEOUT = 0.3


def main():
    args = sys.argv[1:]

    if not args or args[0] in ("-h", "--help"):
        print("Usage: beacon <state> [--direct]")
        print(f"States: {', '.join(sorted(VALID_STATES))}")
        sys.exit(0)

    state = args[0]
    direct = "--direct" in args

    if state not in VALID_STATES:
        # silent — never block Claude Code
        sys.exit(0)

    if direct:
        _send_direct(state)
    else:
        _send_via_socket(state) or _noop()

    sys.exit(0)


def _send_direct(state: str) -> None:
    port = serialio.find_port()
    if port is None:
        return
    serialio.send_direct(state, port, timeout_s=SOCKET_TIMEOUT)


def _send_via_socket(state: str) -> bool:
    """Try to send via daemon socket. Returns True if sent."""
    # M3: implemented in Task 5
    return False


def _noop() -> None:
    pass
```

- [ ] **Step 4: Rodar testes**

```bash
cd host && python -m pytest tests/test_cli.py tests/test_serialio.py -v
```

Saída esperada: todos `PASSED`

- [ ] **Step 5: Smoke test com hardware real**

```bash
beacon --direct thinking   # LED do RP2040 deve ficar amarelo
beacon --direct done       # LED verde + 2 beeps
beacon --direct idle       # LED apaga
```

- [ ] **Step 6: Commit**

```bash
git add host/beacon/cli.py host/tests/test_cli.py
git commit -m "feat(M2): implement CLI --direct mode"
```

---

### Task 4: M3 — send_to_socket em serialio

**Files:**
- Modify: `host/beacon/serialio.py`
- Modify: `host/tests/test_serialio.py`

**Produces:**
- `send_to_socket(state: str, socket_path: str, timeout_s: float = 0.3) -> bool`

- [ ] **Step 1: Adicionar testes de socket em `host/tests/test_serialio.py`**

Adicionar ao final do arquivo:

```python
import socket
import threading


def test_send_to_socket_sends_line():
    """Start a minimal echo server and verify send_to_socket writes state\n."""
    received = []
    ready = threading.Event()

    def server():
        srv = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
        srv.bind("/tmp/beacon_test.sock")
        srv.listen(1)
        ready.set()
        conn, _ = srv.accept()
        data = conn.recv(64)
        received.append(data)
        conn.close()
        srv.close()

    t = threading.Thread(target=server, daemon=True)
    t.start()
    ready.wait(timeout=2)

    result = sio.send_to_socket("thinking", "/tmp/beacon_test.sock")
    t.join(timeout=2)

    assert result is True
    assert received[0] == b"thinking\n"


def test_send_to_socket_returns_false_when_no_daemon():
    result = sio.send_to_socket("thinking", "/tmp/beacon_nonexistent.sock")
    assert result is False
```

- [ ] **Step 2: Rodar para confirmar que falham**

```bash
cd host && python -m pytest tests/test_serialio.py::test_send_to_socket_sends_line tests/test_serialio.py::test_send_to_socket_returns_false_when_no_daemon -v
```

Saída esperada: `FAILED` — função não existe

- [ ] **Step 3: Implementar `send_to_socket` em `host/beacon/serialio.py`**

Adicionar ao final do arquivo:

```python
import socket as _socket


def send_to_socket(state: str, socket_path: str, timeout_s: float = 0.3) -> bool:
    """Send state\n to the daemon Unix socket. Returns True on success."""
    try:
        sock = _socket.socket(_socket.AF_UNIX, _socket.SOCK_STREAM)
        sock.settimeout(timeout_s)
        sock.connect(socket_path)
        sock.sendall(f"{state}\n".encode())
        sock.close()
        return True
    except Exception:
        return False
```

- [ ] **Step 4: Rodar todos os testes**

```bash
cd host && python -m pytest tests/ -v
```

Saída esperada: todos `PASSED`

- [ ] **Step 5: Commit**

```bash
git add host/beacon/serialio.py host/tests/test_serialio.py
git commit -m "feat(M3): implement serialio.send_to_socket"
```

---

### Task 5: M3 — daemon.py

**Files:**
- Modify: `host/beacon/daemon.py`
- Create: `host/tests/test_daemon.py`

**Consumes:**
- `serialio.find_port() -> str | None`
- `serialio.send_direct(state, port, timeout_s)` (não usado — daemon abre a porta ele mesmo)

**Produces:**
- `run(socket_path: str, baud: int = 115200) -> None` — bloqueia; loop infinito
- `_open_serial(port: str, baud: int) -> serial.Serial | None` — tenta abrir, retorna None em falha
- `_serve(socket_path: str, ser: serial.Serial) -> None` — aceita conexões e repassa para serial

- [ ] **Step 1: Escrever testes em `host/tests/test_daemon.py`**

```python
import socket
import threading
import time
from unittest.mock import MagicMock, patch
import beacon.daemon as daemon


def _send(sock_path: str, msg: str) -> None:
    s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
    s.connect(sock_path)
    s.sendall(msg.encode())
    s.close()


def test_daemon_forwards_state_to_serial(tmp_path):
    sock_path = str(tmp_path / "beacon.sock")
    mock_ser = MagicMock()
    mock_ser.is_open = True

    stop = threading.Event()

    def run_daemon():
        with patch("beacon.serialio.find_port", return_value="/dev/cu.fake"), \
             patch("serial.Serial", return_value=mock_ser):
            daemon.run(socket_path=sock_path, stop_event=stop)

    t = threading.Thread(target=run_daemon, daemon=True)
    t.start()
    time.sleep(0.3)  # wait for socket to be ready

    _send(sock_path, "thinking\n")
    time.sleep(0.1)
    stop.set()
    t.join(timeout=2)

    mock_ser.write.assert_called_with(b"thinking\n")


def test_daemon_ignores_invalid_state(tmp_path):
    sock_path = str(tmp_path / "beacon.sock")
    mock_ser = MagicMock()
    mock_ser.is_open = True
    stop = threading.Event()

    def run_daemon():
        with patch("beacon.serialio.find_port", return_value="/dev/cu.fake"), \
             patch("serial.Serial", return_value=mock_ser):
            daemon.run(socket_path=sock_path, stop_event=stop)

    t = threading.Thread(target=run_daemon, daemon=True)
    t.start()
    time.sleep(0.3)

    _send(sock_path, "launch_missiles\n")
    time.sleep(0.1)
    stop.set()
    t.join(timeout=2)

    mock_ser.write.assert_not_called()
```

- [ ] **Step 2: Rodar para confirmar que falham**

```bash
cd host && python -m pytest tests/test_daemon.py -v
```

Saída esperada: `FAILED`

- [ ] **Step 3: Implementar `host/beacon/daemon.py`**

```python
"""Daemon: holds the serial port open and bridges a Unix socket to it."""

import os
import pathlib
import socket
import threading
import time
import logging

import serial

from beacon import serialio

log = logging.getLogger(__name__)

VALID_STATES = {b"thinking", b"waiting", b"done", b"idle", b"error", b"ping"}
SOCKET_PATH = str(pathlib.Path.home() / ".beacon" / "beacon.sock")
RECONNECT_INTERVAL = 2.0


def run(socket_path: str = SOCKET_PATH,
        baud: int = 115200,
        stop_event: threading.Event | None = None) -> None:
    """Main daemon loop. Blocks until stop_event is set (or forever)."""
    pathlib.Path(socket_path).parent.mkdir(parents=True, exist_ok=True)

    # Remove stale socket
    try:
        os.unlink(socket_path)
    except FileNotFoundError:
        pass

    srv = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
    srv.bind(socket_path)
    srv.listen(8)
    srv.settimeout(0.5)

    ser: serial.Serial | None = None
    log.info("Daemon listening on %s", socket_path)

    try:
        while stop_event is None or not stop_event.is_set():
            # Ensure serial is open
            if ser is None or not ser.is_open:
                ser = _connect_serial(baud)

            try:
                conn, _ = srv.accept()
            except socket.timeout:
                continue

            threading.Thread(
                target=_handle_connection,
                args=(conn, ser),
                daemon=True,
            ).start()
    finally:
        srv.close()
        try:
            os.unlink(socket_path)
        except FileNotFoundError:
            pass
        if ser and ser.is_open:
            ser.close()


def _connect_serial(baud: int) -> serial.Serial | None:
    port = serialio.find_port()
    if port is None:
        return None
    try:
        ser = serial.Serial(port, baud, timeout=0.1)
        log.info("Serial connected: %s", port)
        return ser
    except Exception as exc:
        log.warning("Serial open failed (%s), retrying in %.1fs", exc, RECONNECT_INTERVAL)
        time.sleep(RECONNECT_INTERVAL)
        return None


def _handle_connection(conn: socket.socket, ser: serial.Serial | None) -> None:
    try:
        data = conn.recv(64).strip()
        if not data:
            return
        cmd = data.split()[0]
        if cmd not in VALID_STATES:
            log.debug("Ignored unknown command: %r", cmd)
            return
        if ser is None or not ser.is_open:
            log.warning("Serial not available, dropping command %r", cmd)
            return
        ser.write(cmd + b"\n")
    except Exception as exc:
        log.warning("Connection handler error: %s", exc)
    finally:
        conn.close()
```

- [ ] **Step 4: Rodar testes**

```bash
cd host && python -m pytest tests/test_daemon.py -v
```

Saída esperada: todos `PASSED`

- [ ] **Step 5: Commit**

```bash
git add host/beacon/daemon.py host/tests/test_daemon.py
git commit -m "feat(M3): implement daemon with Unix socket and serial bridge"
```

---

### Task 6: M3 — CLI usa socket por padrão, --direct como fallback

**Files:**
- Modify: `host/beacon/cli.py`
- Modify: `host/tests/test_cli.py`

**Consumes:**
- `serialio.send_to_socket(state, socket_path, timeout_s) -> bool`
- `serialio.find_port() -> str | None`
- `serialio.send_direct(state, port, timeout_s) -> bool`

- [ ] **Step 1: Adicionar testes de socket ao `host/tests/test_cli.py`**

Adicionar ao final:

```python
import pathlib
SOCKET_PATH = str(pathlib.Path.home() / ".beacon" / "beacon.sock")


def test_default_tries_socket_first():
    with patch("beacon.serialio.send_to_socket", return_value=True) as mock_sock, \
         patch("beacon.serialio.send_direct") as mock_direct:
        run_cli("thinking")
    mock_sock.assert_called_once_with("thinking", SOCKET_PATH, timeout_s=0.3)
    mock_direct.assert_not_called()


def test_default_silent_if_socket_fails_no_direct():
    with patch("beacon.serialio.send_to_socket", return_value=False), \
         patch("beacon.serialio.find_port", return_value=None):
        code = run_cli("thinking")
    assert code == 0


def test_direct_flag_skips_socket():
    with patch("beacon.serialio.send_to_socket") as mock_sock, \
         patch("beacon.serialio.find_port", return_value="/dev/cu.fake"), \
         patch("beacon.serialio.send_direct", return_value=True) as mock_direct:
        run_cli("done", "--direct")
    mock_sock.assert_not_called()
    mock_direct.assert_called_once_with("done", "/dev/cu.fake", timeout_s=0.3)
```

- [ ] **Step 2: Rodar para confirmar que falham**

```bash
cd host && python -m pytest tests/test_cli.py::test_default_tries_socket_first tests/test_cli.py::test_direct_flag_skips_socket -v
```

Saída esperada: `FAILED`

- [ ] **Step 3: Atualizar `_send_via_socket` em `host/beacon/cli.py`**

Atualizar as imports e a função `_send_via_socket`:

```python
"""CLI entry point for the Status Beacon."""

import pathlib
import sys

from beacon import serialio

VALID_STATES = {"thinking", "waiting", "done", "idle", "error", "ping"}
SOCKET_TIMEOUT = 0.3
SOCKET_PATH = str(pathlib.Path.home() / ".beacon" / "beacon.sock")


def main():
    args = sys.argv[1:]

    if not args or args[0] in ("-h", "--help"):
        print("Usage: beacon <state> [--direct]")
        print(f"States: {', '.join(sorted(VALID_STATES))}")
        sys.exit(0)

    state = args[0]
    direct = "--direct" in args

    if state not in VALID_STATES:
        sys.exit(0)

    if direct:
        _send_direct(state)
    else:
        _send_via_socket(state)

    sys.exit(0)


def _send_direct(state: str) -> None:
    port = serialio.find_port()
    if port is None:
        return
    serialio.send_direct(state, port, timeout_s=SOCKET_TIMEOUT)


def _send_via_socket(state: str) -> None:
    serialio.send_to_socket(state, SOCKET_PATH, timeout_s=SOCKET_TIMEOUT)
```

- [ ] **Step 4: Rodar todos os testes**

```bash
cd host && python -m pytest tests/ -v
```

Saída esperada: todos `PASSED`

- [ ] **Step 5: Smoke test end-to-end com daemon**

```bash
# Terminal 1 — subir o daemon
python -c "from beacon.daemon import run; run()"

# Terminal 2 — disparar estados
beacon thinking   # via socket → daemon → LED amarelo
beacon done       # via socket → daemon → LED verde + beeps
beacon idle       # via socket → daemon → LED apaga
beacon --direct waiting   # bypass do daemon → LED piscando + buzzer
```

- [ ] **Step 6: Commit**

```bash
git add host/beacon/cli.py host/tests/test_cli.py
git commit -m "feat(M3): CLI routes through daemon socket by default"
```

---

### Task 7: Adicionar entry point do daemon ao pyproject.toml

**Files:**
- Modify: `host/pyproject.toml`

- [ ] **Step 1: Adicionar script `beacon-daemon`**

Atualizar a seção `[project.scripts]` no `host/pyproject.toml`:

```toml
[project.scripts]
beacon        = "beacon.cli:main"
beacon-daemon = "beacon.daemon:run"
```

- [ ] **Step 2: Reinstalar o pacote**

```bash
cd host && pip install -e ".[dev]"
```

- [ ] **Step 3: Verificar que os dois comandos estão no PATH**

```bash
which beacon && which beacon-daemon
```

Saída esperada: dois caminhos válidos

- [ ] **Step 4: Smoke test final completo**

```bash
# Subir daemon em background
beacon-daemon &
DAEMON_PID=$!

# Testar todos os estados
beacon thinking && sleep 2
beacon waiting  && sleep 3
beacon done     && sleep 2
beacon idle

# Derrubar daemon
kill $DAEMON_PID

# Testar fallback --direct sem daemon
beacon --direct thinking && sleep 1
beacon --direct idle
```

- [ ] **Step 5: Commit**

```bash
git add host/pyproject.toml
git commit -m "feat(M3): add beacon-daemon entry point to pyproject"
```
